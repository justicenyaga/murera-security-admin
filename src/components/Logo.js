import React from "react";

const Logo = () => {
  return (
    <img
      src="/logo.png"
      alt="logo"
      style={{
        display: "block",
        height: 50,
        marginBottom: 30,
        marginTop: 30,
        marginLeft: "auto",
        marginRight: "auto",
        width: 50,
      }}
    />
  );
};

export default Logo;
